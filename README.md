# deepnets.c
deepnets.c is a low-level C playground for deep neural networks, every forward pass, matrix multiplication, and backpropagation is handcrafted, exposing the raw pointers, memory layouts, and gradient flows that make learning actually happen.
